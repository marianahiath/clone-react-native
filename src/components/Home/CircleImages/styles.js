import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({

    container: {
        flexDirection: 'row',
        justifyContent: 'space-around',
        marginTop: 5
    },

    circles: {
        width: 60,
        height: 60,
        borderRadius: 50
    },

    textMarginTwelve: {
        marginLeft: 12
    },

    textMarginFive: {
        marginLeft: 5
    },

    textSize: {
        fontSize: 12,
        color: '#000'
    }
    
})